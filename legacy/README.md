# Legacy Archive

The files under `legacy/python/` are pre-static implementation snapshots kept for reference only. They were moved out of the runnable project surface because the supported product is the GitHub Pages static dashboard plus userscript connector.

These files are stored with `.txt` suffixes intentionally. Do not treat them as maintained Python modules without first restoring and repairing the old Flask/Selenium application.
